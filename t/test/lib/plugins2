[%# evented capture anon block and edirectives -%]
3[%
NB = BLOCK;
   # INCLUDE named.conf.oklas domains=['one','two'];
   # INSERT a.tmpl;
   USE s = Second;
   EVENT res = s.start(1);
   res.result;
   " ";
   IF 1; 1; ELSE; 0; END;
%]
[% END %] 2 [% NB %]
